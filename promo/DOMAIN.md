# Подключение marylash.eu — ВЫПОЛНЕНО 2026-07-02

Домен куплен владельцем на websupport.sk (до 02.07.2027, аккаунт Dmytro
Baturin). DNS настроен Claude через админку (детали внизу): @ → 4×A GitHub
(185.199.108–111.153), www и * → A 185.199.108.153, @/www/* AAAA →
2606:50c0:8000::153; почтовые записи Websupport не тронуты. CNAME-файл в
репо, custom domain подхвачен, HTTPS enforced. Сайт: https://marylash.eu

Грабли админки Websupport (Quasar SPA), если понадобится снова:
- скриншоты/read_page вкладки не работают (document_idle не наступает);
- кнопки удаления записей не реагируют ни на какие клики (JS/CDP);
- рабочий путь: страницы /sk/dns/{id}/records/{recId}/edit и
  /records/{TYPE}/create — заполнить input[name="dns-content"] нативным
  сеттером + input/change события, потом click() по «Uložiť zmeny»;
- fetch к их внутреннему API из консоли виснет (CSRF) — не тратить время.

---

Исходный план (для истории):

## Шаг 1 — покупка (делает владелец, ~10 минут)

Где: **websupport.sk** (словацкий регистратор, панель по-словацки/по-английски,
.eu ~10 €/год) или **forpsi.sk** (дешевле, ~7 €/год). Оба нормальные —
берите где удобнее.

1. Зайти на сайт регистратора → в поиске доменов ввести `marylash.eu`.
2. Добавить в корзину. Хостинг/e-mail НЕ нужны — только домен
   (сайт уже хостится бесплатно на GitHub Pages).
3. Оплатить картой. Данные владельца домена — ваши (или Марины, если
   хотите оформить на неё сразу; для .eu нужен адрес в ЕС — Словакия ок).

## Шаг 2 — DNS-записи (могу сделать я через браузер, когда вы залогинены)

В панели регистратора → домен marylash.eu → DNS-записи. Добавить:

| Тип   | Имя (host) | Значение |
|-------|-----------|----------|
| A     | @         | 185.199.108.153 |
| A     | @         | 185.199.109.153 |
| A     | @         | 185.199.110.153 |
| A     | @         | 185.199.111.153 |
| CNAME | www       | bambucha0982463-cloud.github.io |

(Если у регистратора уже стоят дефолтные A/AAAA записи на их парковку —
удалить их для @.)

## Шаг 3 — после DNS (делает Claude, ~15 минут + ожидание сертификата)

1. Файл `CNAME` с содержимым `marylash.eu` в корень репо + push.
2. `gh api` — задать custom domain в настройках Pages, включить
   Enforce HTTPS (после выпуска сертификата Let's Encrypt, до ~часа).
3. Обновить в `index.html`: canonical, og:image, Schema.org url/image.
4. Перегенерировать `promo/qr-site.png` на https://marylash.eu/.
5. Обновить ссылку в PROMO.md (для био Инсты и Google Business).
6. Проверить: https://marylash.eu и https://www.marylash.eu отвечают 200,
   старый github.io-адрес редиректит.

## Проверка DNS (для Claude)

`nslookup marylash.eu` → четыре 185.199.x.153;
`nslookup www.marylash.eu` → CNAME на github.io.
